import React from 'react';
// import PropTypes from 'prop-types';

const NavbarMenuEnd = (props) => {
	return <div className={`navbar-end`}>{props.children}</div>;
};

NavbarMenuEnd.propTypes = {};
export default NavbarMenuEnd;
