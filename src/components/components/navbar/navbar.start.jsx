import React from 'react';
// import PropTypes from 'prop-types';

const NavbarMenuStart = (props) => {
	return <div className={`navbar-start`}>{props.children}</div>;
};

NavbarMenuStart.propTypes = {};
export default NavbarMenuStart;
